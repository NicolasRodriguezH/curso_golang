package main

import (
	"fmt"
)

func main() {
	/*

		// Instanciar echo
		e := echo.New()

		// Ruta
		e.GET("/", func(c echo.Context) error {
			return c.String(http.StatusOK, "HELLO WORD")
		})
		e.Logger.Fatal(e.Start(":1323"))

	*/
	fmt.Println("bre")
}
